name: Discord webhook
on:
  release: 
    types: [publish, released, prereleased]

jobs:
  discord_webhook:    
    runs-on: ubuntu-latest    
    steps:
     - name: Discord Webhook Notify
       uses: Cyb3RGER/discord-webhook-notify@af0c8d7ac6f75211f759c0c754a172269299e6a9
       with:
        webhookUrl: ${{ secrets.DISCORD_WEBHOOK_URL }}
